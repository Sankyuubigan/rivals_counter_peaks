# Основные зависимости для тестирования
pytest>=7.0.0
pytest-qt>=4.2.0
pytest-mock>=3.5.0
psutil>=5.9.0

# Для запуска GUI-тестов в headless-режиме (на серверах CI/CD)
# Устанавливать только в Linux-окружении
# pytest-xvfb>=2.0.0

# Зависимости, необходимые для самого приложения,
# которые также используются в тестах.
# Их можно вынести в основной requirements.txt
PyQt5>=5.15.0
opencv-python>=4.6.0
numpy>=1.21.0
onnxruntime>=1.12.0
